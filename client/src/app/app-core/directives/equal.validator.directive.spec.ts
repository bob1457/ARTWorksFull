import { Equal.ValidatorDirective } from './equal.validator.directive';

describe('Equal.ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Equal.ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
